# Python
__pycache__/
*.pyc
*.pyo

# IDE / 系统
.idea/
.vscode/
.DS_Store

# 虚拟环境（如果用 venv 而不是 conda）
.venv/
venv/

# API 凭据：.env 一律不入库，只发 .env.example 占位模板。
# 仓库历史上曾误提交过真实 key，已在 2026-09-04 清理并轮换，勿再放开此行。
.env
.env.*
!.env.example
!.env.*.example
.claude/
