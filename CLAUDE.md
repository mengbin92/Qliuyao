# 量子六爻 · 开发守则

> 这是给 Claude（以及任何在此项目中 AI 协作）的工作协议。每次开发前必须读完本文件并据此执行。

## 一、Skill 加载协议（强制）

任何任务都按下列顺序加载 skill。**找不到任何一个就立刻停下来跟用户对齐，不要在缺失 skill 的情况下盲目执行。**

| 阶段 | Skill | 何时加载 |
|---|---|---|
| 1. 协议建立 | `using-superpowers` | 会话开始 |
| 2. 前端设计辅助 | `frontend-design` + `ui-ux-pro-max` | 涉及前端时 |
| 3. 代码审查 | `simplify` | 写完代码后 |
| 4. 合并门禁（PJR / Pre-Job Review） | `verification-before-merge` | 合并前 |
| 5. 端到端测试 | `webapp-testing`（Playwright） | 涉及前端或后端时 |

---

## 二、工作流（强制顺序）

```
brainstorm（如需要）
  ↓
worktree（创建独立分支，复杂任务必须）
  ↓
开发（遵循开发原则）
  ↓
simplify（代码审查 + 修复）
  ↓
PJR：lint → build → 类型检查 → 逻辑验证
  ↓
端到端测试（Playwright，桌面 + 移动）
  ↓
Vercel 部署（涉及 Web 时）
  ↓
git merge → dev 分支（不直接合 main）
  ↓
（dev → main 由 release 流程统一处理）
```

### 关于 worktree

- **复杂任务必须在 git worktree 里执行**，不要污染主工作目录
- 简单任务（单文件改动、文档修订）可以例外
- 工作完成且 PJR 通过后再合并

---

## 三、开发原则（每条都不可违反）

### 1. 单一职责原则
每个服务、方法只负责一个明确的职责域，避免职责混乱。

### 2. 最简代码原则
不做向后兼容；宁愿破坏性更新也要保证代码最简化；删除所有冗余代码。

### 3. 类型严格原则
所有 TypeScript 类型必须正确，**不使用 `any`**，编译错误必须立即修复。

### 4. KISS 原则
保持简单直接。如果需要解释就是太复杂了。

### 5. 文档置信度原则
绝不基于推测写代码，必须基于真实可验证的技术文档。
特别是涉及支付、数据库、API 等关键功能时，
**如果文档置信度不高，必须停止并要求用户提供准确资料**。

---

## 四、修改原则（强制）

> **打补丁是严格禁止的。**

任何修改、修复、新增内容必须：

- 遵循最佳实践
- **深度融合到代码逻辑内部**（重构 / 调整 / 与现有逻辑结合）
- 从根源上解决问题，不绕过

最终结果必须**简洁却完整地**实现需求。修改过程不必简单，**结果必须最简**。

---

## 五、验收标准（强制）

### 前端

必须用 **Playwright** 做端到端验收，**桌面端和移动端都要测**。

- 不只是页面截图。要把业务链路带入**复杂任务场景**中执行。
- 两类场景必须覆盖：
  1. **正常流程** —— 完整走完用户路径
  2. **边界用例** —— 看是否能触发预期内的异常并被拦截
- 一定要遵循用户流程：**每一个按钮、每一段输入、每一次跳转**都模拟真人走完
- 不只查 UI，**也查 UX**（交互、反馈、节奏）

### 后端

| 类型 | 测试目标 |
|---|---|
| 非 AI 类接口 | 预期正常结果 + 预期异常结果 |
| AI 类接口 | 带入复杂任务，测两件事：① **决策能力**（提示词 + 上下文 + 约束是否够）② **执行能力**（工具调用是否得到 AI 预期的结果） |

涉及前端的任务测前端，涉及后端的任务测后端，**模块级任务两端都测**。

---

## 六、找茬式验证（强制）

我们不是要验证它对，**而是要验证它是否有错**。

必须主动找出下列问题：

- 文字挤压（小屏 / 长文本溢出）
- 表述不一致（跨页面同一概念用不同词）
- 信息密度排版不合理
- 内容不合适（错位、误用术语、产出脏话）
- 无用信息、噪音信息、越权信息
- 圆角、阴影、间距等样式不一致（跨页面）
- 文字是否符合当前场景（占位符、按钮文案、错误提示）

---

## 七、技术与需求的关系

技术方案上**只需要遵循需求**。需要做的是：

- 明确需求里表达的含义
- 保证做出来的链路完整
- 保证流程符合用户思维

代码开发上：**充分探索当前代码** + **遵循开发原则**。

---

## 八、当前项目结构提示

- **`/`**（仓库根）：原版 Python CLI 项目（pyqpanda3 起卦 + DeepSeek 解卦）
- **`/web`**：Next.js 15 网页版（部署到 Vercel），所有前端 + 流式 API 在此
- **`main`**：生产分支
- **`dev`**：集成分支（PR 合并目标）
- **特性分支**：`claude/<task-name>` 或语义命名

---

## 九、Vercel 部署

- CLI 已登录账号：`keith9922`
- 项目根目录：`web`
- 必填环境变量：`DEEPSEEK_API_KEY`
- 可选：`LLM_BASE_URL`、`LLM_MODEL`

部署是**对外可见**的不可逆操作 → **必须通过 PJR 全部通过后再 deploy**。

---

> 写于工作开始之前。修改本文件需经用户同意。
